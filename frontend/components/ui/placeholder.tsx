/**
 * Placeholder component - replace with real UI components in later phases
 */
export function Placeholder({ name }: { name: string }) {
  return (
    <div className="rounded border border-dashed border-gray-300 dark:border-gray-600 p-4 text-sm text-gray-500">
      {name}
    </div>
  );
}
